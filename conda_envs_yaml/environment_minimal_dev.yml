name: morphody_minimal
channels:
#  - defaults
  - conda-forge
  - nodefaults #this is a channel that enforces avoidance of the defaults channel
  - pytorch
  - nvidia
dependencies:
  - python
  - pip
  - bioconda::snakemake
  - ipython
  - pytorch
  - numpy=1.26.4
  - scikit-image
#  - pytorch-cuda=12.4
  - pip:
    - cellpose[gui] #Cellpose recommend python 3.8, but python 3.9 and 3.10 will likely work as well

